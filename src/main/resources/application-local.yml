spring:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true

logging:
  level:
    root: INFO
    org.springframework: INFO
    com.gcs.app: INFO